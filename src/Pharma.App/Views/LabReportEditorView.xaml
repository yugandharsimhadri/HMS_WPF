<UserControl x:Class="Pharma.App.Views.LabReportEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Pharma.App"
             Background="{DynamicResource Bg}"
             Width="480">

    <DockPanel Margin="18">

        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,14" Padding="16,12">
            <TextBlock Text="{Binding Header}" Style="{StaticResource H2}"
                       TextTrimming="CharacterEllipsis"
                       AutomationProperties.AutomationId="ReportEditorHeader"/>
        </Border>

        <Border DockPanel.Dock="Bottom" Margin="0,14,0,0">
            <DockPanel>
                <Button DockPanel.Dock="Left" Content="Delete" Style="{StaticResource BtnDanger}"
                        MinWidth="100"
                        Command="{Binding DeleteCommand}"
                        Visibility="{Binding CanDelete, Converter={StaticResource BoolVis}}"
                        AutomationProperties.AutomationId="ReportEditorDelete"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource BtnGhost}" Margin="0,0,8,0"
                            MinWidth="100" IsCancel="True"
                            Command="{Binding CancelCommand}"
                            AutomationProperties.AutomationId="ReportEditorCancel"/>
                    <Button Content="Save report" Style="{StaticResource BtnPrimaryLarge}"
                            MinWidth="130" IsDefault="True"
                            Command="{Binding SaveCommand}"
                            AutomationProperties.AutomationId="ReportEditorSave"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <TextBlock DockPanel.Dock="Bottom" Text="{Binding Status}" Style="{StaticResource Muted}"
                   TextWrapping="Wrap" Margin="2,10,2,0"
                   AutomationProperties.AutomationId="ReportEditorStatus"/>

        <Border Style="{StaticResource Card}">
            <StackPanel>
                <local:RequiredLabel Text="Report name"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         local:Field.IsMissing="{Binding NameMissing}"
                         AutomationProperties.AutomationId="ReportEditorName"/>

                <TextBlock Text="Category" Style="{StaticResource Label}"/>
                <ComboBox ItemsSource="{Binding Categories}" Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"
                          IsEditable="True"
                          AutomationProperties.AutomationId="ReportEditorCategory"/>

                <TextBlock Text="Price (whole panel)" Style="{StaticResource Label}"/>
                <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"
                         local:NumericInput.Mode="Money"
                         AutomationProperties.AutomationId="ReportEditorPrice"/>

                <CheckBox Content="Active" IsChecked="{Binding Active, Mode=TwoWay}"
                          Margin="0,10,0,10"
                          AutomationProperties.AutomationId="ReportEditorActive"/>

                <TextBlock Text="Analytes in this panel" Style="{StaticResource H2}" Margin="0,4,0,6"/>

                <DockPanel Margin="0,0,0,8">
                    <Button DockPanel.Dock="Right" Content="Add" Style="{StaticResource BtnPrimary}"
                            Margin="8,0,0,0"
                            Command="{Binding AddMemberCommand}"
                            AutomationProperties.AutomationId="ReportEditorAddMember"/>
                    <ComboBox ItemsSource="{Binding Analytes}" SelectedItem="{Binding SelectedAnalyteToAdd, Mode=TwoWay}"
                              DisplayMemberPath="Name"
                              AutomationProperties.AutomationId="ReportEditorAnalytePicker"/>
                </DockPanel>

                <ItemsControl ItemsSource="{Binding Members}"
                              AutomationProperties.AutomationId="ReportEditorMembers">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel Margin="0,2,0,2">
                                <Button DockPanel.Dock="Right" Content="✕" BorderThickness="0"
                                        Background="Transparent" Foreground="{DynamicResource Danger}" Cursor="Hand"
                                        Command="{Binding DataContext.RemoveMemberCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                                <TextBlock Text="{Binding Units}" DockPanel.Dock="Right" Margin="0,0,8,0" Style="{StaticResource Muted}"/>
                                <TextBlock Text="{Binding Name}"/>
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </DockPanel>
</UserControl>
