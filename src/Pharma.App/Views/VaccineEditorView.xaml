<UserControl x:Class="Pharma.App.Views.VaccineEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Pharma.App"
             Background="{DynamicResource Bg}"
             Width="420">

    <DockPanel Margin="18">

        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,14" Padding="16,12">
            <TextBlock Text="{Binding Header}" Style="{StaticResource H2}"
                       TextTrimming="CharacterEllipsis"
                       AutomationProperties.AutomationId="VaccineEditorHeader"/>
        </Border>

        <Border DockPanel.Dock="Bottom" Margin="0,14,0,0">
            <DockPanel>
                <Button DockPanel.Dock="Left" Content="Delete" Style="{StaticResource BtnDanger}"
                        MinWidth="100"
                        Command="{Binding DeleteCommand}"
                        Visibility="{Binding CanDelete, Converter={StaticResource BoolVis}}"
                        AutomationProperties.AutomationId="VaccineEditorDelete"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource BtnGhost}" Margin="0,0,8,0"
                            MinWidth="100" IsCancel="True"
                            Command="{Binding CancelCommand}"
                            AutomationProperties.AutomationId="VaccineEditorCancel"/>
                    <Button Content="Save vaccine" Style="{StaticResource BtnPrimaryLarge}"
                            MinWidth="130" IsDefault="True"
                            Command="{Binding SaveCommand}"
                            AutomationProperties.AutomationId="VaccineEditorSave"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <TextBlock DockPanel.Dock="Bottom" Text="{Binding Status}" Style="{StaticResource Muted}"
                   TextWrapping="Wrap" Margin="2,10,2,0"
                   AutomationProperties.AutomationId="VaccineEditorStatus"/>

        <Border Style="{StaticResource Card}">
            <StackPanel>
                <local:RequiredLabel Text="Vaccine name"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         local:Field.IsMissing="{Binding NameMissing}"
                         AutomationProperties.AutomationId="VaccineEditorName"/>

                <TextBlock Text="Category" Style="{StaticResource Label}"/>
                <ComboBox ItemsSource="{Binding Categories}" Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"
                          IsEditable="True"
                          AutomationProperties.AutomationId="VaccineEditorCategory"/>

                <TextBlock Text="Dose number" Style="{StaticResource Label}"/>
                <TextBox Text="{Binding DoseNumber, UpdateSourceTrigger=PropertyChanged}"
                         local:NumericInput.Mode="Whole"
                         AutomationProperties.AutomationId="VaccineEditorDoseNumber"/>

                <TextBlock Text="Recommended age (days from birth)" Style="{StaticResource Label}"/>
                <TextBox Text="{Binding RecommendedAgeDays, UpdateSourceTrigger=PropertyChanged}"
                         local:NumericInput.Mode="Whole"
                         ToolTip="e.g. 42 for a 6-week dose, 0 for at birth"
                         AutomationProperties.AutomationId="VaccineEditorAgeDays"/>

                <CheckBox Content="Active" IsChecked="{Binding Active, Mode=TwoWay}"
                          Margin="0,10,0,4"
                          AutomationProperties.AutomationId="VaccineEditorActive"/>
            </StackPanel>
        </Border>
    </DockPanel>
</UserControl>
